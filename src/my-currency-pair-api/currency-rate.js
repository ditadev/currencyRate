const displayRate = require('./display-rate');
const getRate = require ('./get-rate');
const prompt = require('prompt-sync')();

const curPair = prompt("Kindly enter currency pair ");

displayRate();
